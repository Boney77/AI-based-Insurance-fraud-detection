# Local dev: Vite proxy forwards /api → localhost:8080 automatically.
# No .env needed for local development.

# Production (Vercel): set this in Vercel Dashboard → Settings → Environment Variables
# RAILWAY_BACKEND_URL=https://your-app-name.up.railway.app
# (NO trailing slash)
